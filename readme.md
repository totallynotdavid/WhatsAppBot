## Prerrequisites:

1. ffmpeg

## Steps

1. Create an `.env` file with the following variables: 

   - `spotifyClientId`
   - `spotifyClientSecret`
   - `youtubeKey`
  
2. Run the following commands:
   
   - `npm install`
   - `npm run start`